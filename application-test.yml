

spring:
  application:
    name: config-server-test
  cloud:
    config:
      server:
        git:
          uri: git@github.com:fypshencheng/spring-cloud-congfig-server-demo.git
server:
  port: 9091
